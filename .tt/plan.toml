schema = "tt-managed-project-plan-v1"
status = "draft"
objective = "Coordinate dev, test, and integration work for tt"
updated_at = "2026-04-10T01:18:28.725954280+00:00"

[[milestones]]
id = "milestone-1"
title = "Plan and scope the work"
success_criteria = [
    "The director has an explicit plan",
    "Workers have clear ownership",
]
evidence = []

[[milestones]]
id = "milestone-2"
title = "Implement and validate the change set"
success_criteria = [
    "The director has an explicit plan",
    "Workers have clear ownership",
]
evidence = []

[[milestones]]
id = "milestone-3"
title = "Prepare merge readiness"
success_criteria = [
    "The director has an explicit plan",
    "Workers have clear ownership",
]
evidence = []

[[work_items]]
id = "tt-director"
title = "Director"
owner_role = "director"
phase = "plan"
depends_on = []
acceptance_criteria = [
    "The handoff is explicit",
    "Validation evidence is recorded",
]
validation_commands = ["cargo test"]
commit_required = false
status = "planned"

[[work_items]]
id = "tt-dev"
title = "Developer"
owner_role = "dev"
phase = "develop"
depends_on = ["tt-director"]
acceptance_criteria = [
    "The handoff is explicit",
    "Validation evidence is recorded",
]
validation_commands = ["cargo test"]
commit_required = true
status = "planned"

[[work_items]]
id = "tt-test"
title = "Test"
owner_role = "test"
phase = "test"
depends_on = ["tt-dev"]
acceptance_criteria = [
    "The handoff is explicit",
    "Validation evidence is recorded",
]
validation_commands = ["cargo test"]
commit_required = true
status = "planned"

[[work_items]]
id = "tt-integration"
title = "Integration"
owner_role = "integration"
phase = "integrate"
depends_on = ["tt-test"]
acceptance_criteria = [
    "The handoff is explicit",
    "Validation evidence is recorded",
]
validation_commands = ["cargo test"]
commit_required = true
status = "planned"

[notes]
risks = []
pitfalls = []
open_questions = [
    "What exact scope and non-goals should the director enforce for `tt`?",
    "Which validation commands are required before the director can mark the plan ready to dispatch?",
    "What repo-specific pitfalls or exceptions should the director carry forward during execution?",
    "What operator approval or landing gate must be satisfied before merge?",
]
operator_constraints = []
