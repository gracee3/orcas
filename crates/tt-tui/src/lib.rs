//! TUI frontend for TT v2.
//!
//! The initial v2 operator experience is TUI-first and should depend on shared
//! view models rather than direct storage access.

use anyhow as _;
use tt_ui_core as _;

pub const TT_TUI_PRODUCT: &str = "tt-tui";
