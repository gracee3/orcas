NAME=hello
MODE=deterministic
TAGS=deterministic,smoke,hello
DEFAULT_ENABLED=true
TIMEOUT_SECONDS=120
REQUIRES_RUNTIME=false
REQUIRES_NETWORK=false
REQUIRES_CLEAN_GIT=false
REQUIRES_EXTRACTED_HANDOFFS=false
